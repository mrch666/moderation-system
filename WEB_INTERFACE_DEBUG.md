# 🔧 ОТЛАДКА ВЕБ-ИНТЕРФЕЙСА

Если очередь не отображается в веб-интерфейсе, выполните следующие шаги:

## 📋 БЫСТРАЯ ПРОВЕРКА:

### 1. Откройте веб-интерфейс:
```
http://192.168.1.189:8080
```

### 2. Откройте консоль разработчика (F12):
- Перейдите на вкладку **Console**
- Проверьте ошибки JavaScript
- Должны быть сообщения о загрузке config.js и очереди

### 3. Проверьте вкладку Network:
- Фильтруйте запросы по `/api`
- Должны быть запросы к `/api/moderation/queue`
- Проверьте статус ответов (должен быть 200 OK)
- Проверьте содержимое ответов (должны быть данные)

## 🐛 РАСПРОСТРАНЕННЫЕ ПРОБЛЕМЫ:

### Проблема 1: config.js не загружается
**Симптомы:**
- В консоли: `Failed to load resource: the server responded with a status of 404 (Not Found)`
- Запрос к `/config.js` возвращает 404

**Решение:**
```bash
# Перезапустите frontend
cd moderation-system
./manage.sh restart
```

### Проблема 2: CORS ошибки
**Симптомы:**
- В консоли: `Access to fetch at 'http://localhost:3000/api/moderation/queue' from origin 'http://localhost:8080' has been blocked by CORS policy`
- Запросы блокируются браузером

**Решение:**
- Убедитесь, что backend запущен с правильными CORS настройками
- Backend должен возвращать заголовки:
  ```
  Access-Control-Allow-Origin: http://localhost:8080
  Access-Control-Allow-Credentials: true
  ```

### Проблема 3: API не отвечает
**Симптомы:**
- Запросы к API зависают или возвращают ошибки
- В консоли: `Failed to fetch` или таймауты

**Решение:**
```bash
# Проверьте работу backend
curl -s "http://localhost:3000/health"
curl -s "http://localhost:3000/api/moderation/queue?limit=1" -H "X-API-Key: test_api_key_123456"
```

### Проблема 4: Пустая очередь
**Симптомы:**
- API возвращает `{"success":true,"data":[],"pagination":{...}}`
- Массив `data` пустой

**Решение:**
- Создайте тестовую модерацию:
```bash
cd moderation-system
./test_simple.sh
```

## 🧪 ТЕСТОВЫЕ СКРИПТЫ:

### Проверка всей цепочки:
```bash
./test-web-interface.sh
```

### Проверка API:
```bash
./test_simple.sh
```

### Проверка конфигурации:
```bash
# Проверьте config.js
curl -s "http://localhost:8080/config.js"

# Проверьте API
curl -s "http://localhost:3000/health"
```

## 📊 ТЕКУЩИЕ НАСТРОЙКИ:

### Frontend:
- URL: `http://192.168.1.189:8080`
- Config: `http://192.168.1.189:8080/config.js`
- API URL: `http://localhost:3000/api`

### Backend:
- URL: `http://192.168.1.189:3000`
- Health: `http://192.168.1.189:3000/health`
- API Key: `test_api_key_123456`

### Telegram:
- Бот: `@imagesmoderatesystem_bot`
- Группа: `imagemoderationgrup` (ID: -4971670825)

## 🚀 БЫСТРОЕ ИСПРАВЛЕНИЕ:

Если ничего не помогает, выполните полный перезапуск:

```bash
cd moderation-system
./manage.sh stop
sleep 3
./manage.sh start
sleep 5
```

Затем откройте веб-интерфейс и проверьте консоль разработчика.

---

**Если проблема persists, предоставьте:**
1. Скриншот консоли разработчика (вкладка Console)
2. Скриншот вкладки Network (фильтр по /api)
3. Логи из `backend/backend.log`
4. Логи из `simple-frontend/frontend.log`