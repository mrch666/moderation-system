# 🚀 Быстрый старт системы модерации

## 📋 Предварительные требования
- Node.js 18+
- npm
- Доступ в интернет (для загрузки зависимостей)

## 🎯 Быстрый запуск

### 1. Перейдите в папку проекта
```bash
cd moderation-system
```

### 2. Запустите систему одним скриптом
```bash
./run_system.sh
```

### 3. Откройте веб-интерфейс
Откройте в браузере: http://localhost:8080

## 🔧 Ручной запуск компонентов

### Запуск Backend API:
```bash
cd backend
npm start
```
Backend будет доступен по адресу: http://localhost:3000

### Запуск простого фронтенда:
```bash
cd simple-frontend
node server.js
```
Фронтенд будет доступен по адресу: http://localhost:8080

### Запуск Telegram бота (опционально):
```bash
cd telegram-bot
npm start
```
Предварительно настройте токен бота в файле `telegram-bot/.env`

## 🧪 Тестирование системы

### Быстрый тест API:
```bash
./test_simple.sh
```

### Ручное тестирование API:

#### Проверка здоровья:
```bash
curl http://localhost:3000/health
```

#### Отправка на модерацию:
```bash
curl -X POST http://localhost:3000/api/moderation/submit \
  -H "X-API-Key: test_api_key_123456" \
  -H "Content-Type: application/json" \
  -d '{
    "image_url": "https://picsum.photos/800/600",
    "product_id": "TEST-001",
    "download_url": "https://picsum.photos/800/600"
  }'
```

#### Получение очереди:
```bash
curl -H "X-API-Key: test_api_key_123456" \
  http://localhost:3000/api/moderation/queue
```

## ⚙️ Настройка

### 1. API ключи
По умолчанию используется ключ: `test_api_key_123456`

Для изменения отредактируйте:
- `backend/.env` - `API_KEY`
- `telegram-bot/.env` - `API_KEY`

### 2. Telegram бот
1. Создайте бота через @BotFather
2. Получите токен
3. Отредактируйте `telegram-bot/.env`:
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   BOT_ADMIN_CHAT_ID=ваш_chat_id
   ```

### 3. База данных
Система использует встроенную SQLite для простоты. Для production рекомендуется PostgreSQL.

## 🌐 Веб-интерфейс

### Основные функции:
1. **Дашборд** - статистика системы
2. **Очередь модерации** - просмотр и модерация изображений
3. **Отправка на модерацию** - форма для отправки новых изображений
4. **Настройки** - управление API ключами и URL

### Использование:
1. Откройте http://localhost:8080
2. API ключ уже введен по умолчанию
3. Для отправки на модерацию:
   - Перейдите во вкладку "Отправить на модерацию"
   - Заполните форму
   - Нажмите "Отправить на модерацию"
4. Для модерации:
   - Перейдите во вкладку "Очередь модерации"
   - Нажмите ✅ для одобрения или ❌ для отклонения

## 🐛 Устранение неполадок

### Backend не запускается:
```bash
cd backend
npm install
npm start
```

### Фронтенд не открывается:
```bash
cd simple-frontend
node server.js
```

### Ошибки API:
1. Проверьте, что backend запущен: `curl http://localhost:3000/health`
2. Проверьте API ключ в веб-интерфейсе
3. Проверьте логи backend

### Просмотр логов:
```bash
# Backend логи будут выводиться в консоль
# При запуске через ./run_system.sh логи всех компонентов видны в консоли
```

## 📊 Примеры использования

### Интеграция с другим приложением:
```javascript
// JavaScript пример
async function submitForModeration(imageUrl, productId) {
    const response = await fetch('http://localhost:3000/api/moderation/submit', {
        method: 'POST',
        headers: {
            'X-API-Key': 'test_api_key_123456',
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({
            image_url: imageUrl,
            product_id: productId,
            download_url: imageUrl
        })
    });
    return await response.json();
}
```

### Проверка статуса:
```bash
# Получить статус по UUID
curl http://localhost:3000/api/moderation/status/<uuid>
```

## 🔒 Безопасность

### Для production использования:
1. Измените API ключи в `.env` файлах
2. Настройте HTTPS
3. Ограничьте доступ к API
4. Регулярно обновляйте зависимости

## 📞 Поддержка

При возникновении проблем:
1. Проверьте, что все порты свободны (3000, 8080)
2. Проверьте подключение к интернету
3. Проверьте логи в консоли
4. Убедитесь, что Node.js версии 18+

## 🎉 Поздравляем!
Система модерации готова к использованию. Вы можете:
- Отправлять изображения на модерацию через API
- Модерировать через веб-интерфейс
- Получать статистику работы
- Интегрировать с другими системами