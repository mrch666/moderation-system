# ⚙️ Настройка GitHub Actions для системы модерации

## 📋 Обзор

GitHub Actions автоматизирует:
- ✅ Тестирование кода
- ✅ Сборку Docker образов
- ✅ Развертывание на сервер
- ✅ Отправку уведомлений

## 🚀 Быстрая настройка

### Шаг 1: Перейдите в настройки секретов
```
https://github.com/mrch666/moderation-system/settings/secrets/actions
```

### Шаг 2: Добавьте необходимые секреты

#### Обязательные секреты:
| Секрет | Описание | Пример значения |
|--------|----------|-----------------|
| `API_KEY` | Ключ API для продакшена | `production_api_key_secure_123` |
| `TARGET_SERVER_URL` | URL целевого сервера для загрузки изображений | `http://img.instrumentstore.ru:7990/api/modelgoods/image/` |

#### Опциональные секреты (для Telegram):
| Секрет | Описание | Как получить |
|--------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | Через @BotFather |
| `TELEGRAM_CHAT_ID` | ID чата для уведомлений | Через @userinfobot |

### Шаг 3: Добавьте секреты через интерфейс

1. Нажмите **"New repository secret"**
2. Введите **Name** (например: `API_KEY`)
3. Введите **Value** (ваш ключ API)
4. Нажмите **"Add secret"**

## 🔧 Детальная настройка

### 1. Секрет API_KEY
```bash
# Генерация надежного ключа
openssl rand -base64 32
# Результат: aBcDeFgHiJkLmNoPqRsTuVwXyZ0123456789+=

# Или используйте:
production_api_key_$(openssl rand -hex 16)
```

### 2. Секрет TARGET_SERVER_URL
```
http://img.instrumentstore.ru:7990/api/modelgoods/image/
```

### 3. Секреты для Telegram (опционально)

#### Получение TELEGRAM_BOT_TOKEN:
1. Напишите @BotFather в Telegram
2. Отправьте `/newbot`
3. Укажите имя бота (например: `Moderation System Bot`)
4. Получите токен (например: `1234567890:ABCdefGHIjklMNopQRstUVwxyz`)
5. Скопируйте токен в секрет `TELEGRAM_BOT_TOKEN`

#### Получение TELEGRAM_CHAT_ID:
1. Напишите @userinfobot в Telegram
2. Отправьте `/start`
3. Получите ваш Chat ID
4. Скопируйте ID в секрет `TELEGRAM_CHAT_ID`

## ⚡ Включение GitHub Actions

1. Перейдите во вкладку **Actions**:
   ```
   https://github.com/mrch666/moderation-system/actions
   ```

2. Нажмите зеленую кнопку:
   **"I understand my workflows, go ahead and enable them"**

3. Дождитесь первого запуска workflow (может занять 1-2 минуты)

## 🧪 Тестирование GitHub Actions

### Ручной запуск workflow:
1. Перейдите в **Actions**
2. Выберите **"CI/CD Pipeline"**
3. Нажмите **"Run workflow"**
4. Выберите ветку (например: `main`)
5. Нажмите **"Run workflow"**

### Проверка результатов:
1. После запуска нажмите на workflow
2. Просмотрите шаги выполнения
3. Проверьте логи на ошибки

## 🐳 Настройка Docker Registry (опционально)

Если хотите автоматически собирать Docker образы:

### Для Docker Hub:
```bash
# Добавьте секреты:
DOCKER_USERNAME=ваш_docker_username
DOCKER_PASSWORD=ваш_docker_password
```

### Для GitHub Container Registry:
```bash
# Добавьте секрет:
CR_PAT=ваш_github_personal_access_token_с_правами_packages
```

## 🔐 Настройка окружения разработки

### Локальная разработка:
```bash
# Копируем пример конфигурации
cp .env.example .env.development

# Редактируем конфигурацию
nano .env.development

# Основные настройки:
PORT=3000
API_KEY=test_api_key_123456
TARGET_SERVER_URL=http://img.instrumentstore.ru:7990/api/modelgoods/image/
CORS_ORIGIN=http://localhost:8080
```

### Продакшн настройки (в секретах GitHub):
```bash
API_KEY=production_api_key_secure_$(openssl rand -hex 16)
TARGET_SERVER_URL=http://img.instrumentstore.ru:7990/api/modelgoods/image/
```

## 📊 Мониторинг и уведомления

### Настройка уведомлений в Telegram:
1. Добавьте секреты `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
2. GitHub Actions будет отправлять уведомления:
   - При успешном деплое ✅
   - При ошибке сборки ❌
   - При завершении тестов 📊

### Настройка уведомлений в Slack (опционально):
1. Добавьте секрет `SLACK_WEBHOOK_URL`
2. Получите webhook URL из Slack App

## 🛡️ Защита веток

### Рекомендуемые настройки:
1. Перейдите: **Settings → Branches**
2. Нажмите: **"Add branch protection rule"**
3. Настройте для ветки `main`:
   - ✅ Require pull request reviews before merging
   - ✅ Require status checks to pass before merging
   - ✅ Include administrators
   - ✅ Require conversation resolution before merging
4. Нажмите: **"Create"**

### Настройка для ветки `develop`:
1. Создайте правило для `develop`
2. Настройте:
   - ✅ Require status checks to pass before merging
   - ✅ Require branches to be up to date before merging

## 🔄 Автоматическое развертывание

### Настройка деплоя на сервер:
1. Добавьте секреты для доступа к серверу:
   ```bash
   DEPLOY_HOST=ваш_сервер.com
   DEPLOY_USER=username
   DEPLOY_SSH_KEY=приватный_ssh_ключ
   ```

2. Настройте workflow для автоматического деплоя при пуше в `main`

### Настройка staging окружения:
1. Создайте ветку `staging`
2. Настройте автоматический деплой на staging при пуше в `staging`
3. Добавьте соответствующие секреты для staging сервера

## 🐛 Устранение неполадок

### Проблема: Workflow не запускается
**Решение:**
1. Проверьте, что файл `.github/workflows/ci-cd.yml` существует
2. Проверьте синтаксис YAML файла
3. Убедитесь, что Actions включены в репозитории

### Проблема: Ошибка "Secrets not found"
**Решение:**
1. Проверьте, что все необходимые секреты добавлены
2. Проверьте имена секретов (чувствительны к регистру)
3. Перезапустите workflow

### Проблема: Docker build fails
**Решение:**
1. Проверьте Dockerfile на ошибки
2. Проверьте, что все зависимости указаны
3. Проверьте квоты Docker Hub/GitHub Packages

### Проблема: Тесты не проходят
**Решение:**
1. Проверьте логи тестов
2. Запустите тесты локально: `./test_simple.sh`
3. Исправьте ошибки в коде

## 📈 Оптимизация

### Кэширование зависимостей:
Workflow уже настроен на кэширование:
- Node.js dependencies
- Docker layers
- Build artifacts

### Параллельное выполнение:
Workflow выполняет:
- ✅ Linting и тесты параллельно
- ✅ Docker builds параллельно
- ✅ Deploy только после успешных тестов

### Уведомления:
- Telegram при успехе/ошибке
- Status badges в README.md
- Deployment status в PR

## 🎯 Готовые команды

### Локальный тест workflow:
```bash
# Установите act для локального тестирования GitHub Actions
brew install act  # macOS
# или
curl -s https://raw.githubusercontent.com/nektos/act/master/install.sh | sudo bash

# Запустите локальный тест
act -s API_KEY=test_key -s TARGET_SERVER_URL=http://test.server
```

### Проверка секретов:
```bash
# Список секретов (требуется GitHub CLI)
gh secret list

# Добавление секрета через CLI
gh secret set API_KEY --body "your_api_key"
```

### Ручной запуск деплоя:
```bash
# Через GitHub CLI
gh workflow run "CI/CD Pipeline"
```

## 📞 Поддержка

### Полезные ссылки:
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Docker Documentation](https://docs.docker.com/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

### Отладка:
1. Проверьте логи workflow в GitHub
2. Запустите тесты локально
3. Проверьте конфигурацию окружения

---

**🎉 Настройка завершена! Система готова к автоматическому тестированию и развертыванию.**