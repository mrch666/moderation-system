# 🧪 ТЕСТИРОВАНИЕ КНОПОК В ВЕБ-ИНТЕРФЕЙСЕ

## 🚀 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ:

### 1. Откройте веб-интерфейс:
```
http://192.168.1.189:8080
```

### 2. Откройте консоль разработчика:
- Нажмите **F12** или **Ctrl+Shift+I**
- Перейдите на вкладку **Console**

### 3. Проверьте загрузку конфигурации:
В консоли должно быть сообщение:
```
🚀 Конфигурация загружена: {API_URL: "...", API_KEY: "...", DEBUG: true}
🌐 API URL: http://localhost:3000/api
```

### 4. Нажмите кнопку "Approve" или "Reject":
- Найдите любой товар в очереди
- Нажмите кнопку **✅ Approve** или **❌ Reject**

### 5. Проверьте консоль:
Должны появиться сообщения:
```
🔍 DEBUG: Нажата кнопка moderate, id: ..., status: approved/rejected
🔍 DEBUG: Отправляю запрос на /moderation/.../moderate
🔍 DEBUG: makeRequest: PUT http://localhost:3000/api/moderation/.../moderate
🔍 DEBUG: Response status: 200 OK
🔍 DEBUG: API success: {success: true, ...}
```

## 🔧 ЕСЛИ КНОПКИ НЕ РАБОТАЮТ:

### Проблема 1: Нет сообщений в консоли
**Симптом:** При нажатии кнопки ничего не происходит, нет сообщений в консоли.

**Возможные причины:**
1. JavaScript не выполняется
2. Ошибка при загрузке страницы
3. Кнопки не привязаны к обработчикам

**Решение:**
1. Проверьте вкладку **Console** на ошибки загрузки
2. Обновите страницу (Ctrl+F5)
3. Проверьте, загрузился ли `config.js`

### Проблема 2: Ошибки в консоли
**Симптом:** В консоли есть ошибки JavaScript.

**Что проверять:**
1. Ошибки типа `Uncaught ReferenceError: API_KEY is not defined`
2. Ошибки CORS
3. Ошибки сети

**Решение:**
1. Проверьте загрузку `config.js`
2. Проверьте CORS заголовки от backend
3. Проверьте доступность API

### Проблема 3: Запрос отправляется, но ошибка
**Симптом:** В консоли видно, что запрос отправляется, но возвращается ошибка.

**Что проверять:**
1. Статус ответа (не 200)
2. Текст ошибки
3. Заголовки запроса

**Решение:**
1. Проверьте API ключ в заголовках
2. Проверьте endpoint в backend
3. Проверьте логи backend

## 📋 БЫСТРАЯ ПРОВЕРКА API:

### Проверка endpoint модерации:
```bash
# Получите ID первой модерации
ID=$(curl -s "http://localhost:3000/api/moderation/queue?limit=1" -H "X-API-Key: test_api_key_123456" | grep -o '"moderation_uuid":"[^"]*"' | head -1 | cut -d'"' -f4)

# Попробуйте одобрить
curl -X PUT "http://localhost:3000/api/moderation/$ID/moderate" \
  -H "Content-Type: application/json" \
  -H "X-API-Key: test_api_key_123456" \
  -d '{"status": "approved"}'
```

### Проверка конфигурации:
```bash
# Проверьте config.js
curl -s "http://localhost:8080/config.js"

# Проверьте переменные
echo "API_URL: http://localhost:3000/api"
echo "API_KEY: test_api_key_123456"
```

## 🎯 ЧТО ДОЛЖНО ПРОИСХОДИТЬ ПРИ УСПЕШНОЙ РАБОТЕ:

1. ✅ Страница загружается без ошибок
2. ✅ Очередь отображается с изображениями
3. ✅ В консоли есть сообщение о загрузке конфигурации
4. ✅ При нажатии кнопки появляется confirm диалог (для Approve)
5. ✅ В консоли появляются отладочные сообщения
6. ✅ Запрос отправляется на API
7. ✅ API возвращает успешный ответ
8. ✅ Очередь обновляется автоматически
9. ✅ Появляется уведомление об успехе

## 📞 ЕСЛИ НИЧЕГО НЕ ПОМОГАЕТ:

Предоставьте:
1. Скриншот консоли разработчика
2. Скриншот вкладки Network (фильтр по /api)
3. Логи из `backend/backend.log`
4. Логи из `simple-frontend/frontend.log`

**Команда для сбора логов:**
```bash
cd moderation-system
tail -50 backend/backend.log
tail -50 simple-frontend/frontend.log
```

---

**Откройте веб-интерфейс, нажмите кнопку и сообщите, что видите в консоли!**